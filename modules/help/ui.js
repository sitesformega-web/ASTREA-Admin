function renderHelpModule(container) {
  container.innerHTML = `
    <section class="admin-card">

      <h2>❓ Ayuda</h2>

      ${renderEmptyState("Módulo en construcción.")}

    </section>
  `;
}